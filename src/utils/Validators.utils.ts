import { check } from 'express-validator';
export class BaseValidator {

}


// export class Validator {
// 	// validate: length, string, number, minlength, maxlength, email (RegEX), required (not null)

// }